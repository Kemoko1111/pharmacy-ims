{
  "name": "pharmatrack",
  "private": true,
  "version": "0.1.0",
  "description": "PharmaTrack — pharmacy inventory & POS (COE 454)",
  "scripts": {
    "dev": "pnpm --parallel --filter @pharmatrack/api --filter @pharmatrack/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  }
}
